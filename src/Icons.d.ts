import { SVGTemplateResult } from 'lit';

/**
 * Wraps icon into an SVG container.
 * @param tpl Icon definition
 * @param width The icon view box width
 * @param height The icon view box height
 * @returnsComplete SVG icon definition
 */
export function iconWrapper(tpl: SVGTemplateResult, width?: number, height?: number): SVGTemplateResult;
export const arrowRight: SVGTemplateResult;
export const check: SVGTemplateResult;
